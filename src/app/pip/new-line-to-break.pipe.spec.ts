import { NewLineToBreakPipe } from './new-line-to-break.pipe';

describe('NewLineToBreakPipe', () => {
  it('create an instance', () => {
    const pipe = new NewLineToBreakPipe();
    expect(pipe).toBeTruthy();
  });
});
